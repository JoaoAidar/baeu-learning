# Leave empty in dev when Vite proxies /api to the local backend.
# Production canonical backend:
VITE_API_BASE_URL=https://baeu-backend-production.up.railway.app
# Enable only after backend GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are set.
VITE_GOOGLE_SIGN_IN_ENABLED=false
