PORT=3001
NODE_ENV=development

# Auth
JWT_SECRET=replace-me-with-32-bytes-random
JWT_EXPIRES_IN=7d
ADMIN_TOKEN=dev-admin

# Allowed frontend origin in production (comma-separated). Empty = allow all (dev).
# Production frontend: https://baeu-learning.vercel.app
CORS_ORIGIN=

# Neon Postgres connection. Leave empty to use the in-memory store.
# Example: postgresql://user:pass@ep-xxx.region.aws.neon.tech/baeu?sslmode=require
DATABASE_URL=

# LLM exercise generator (OpenRouter or compatible). Empty = /admin/exercises/generate disabled.
LLM_API_KEY=
LLM_BASE_URL=https://openrouter.ai/api/v1/chat/completions
LLM_MODEL=anthropic/claude-3.5-sonnet
